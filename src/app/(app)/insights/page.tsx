import { PageStub } from "@/components/shell/page-stub";

export default function InsightsPage() {
  return (
    <PageStub
      title="Smart Insights"
      points={[
        "Daily AI-generated insights on concentration, idle cash, allocation drift",
        "Emergency-fund coverage and equity-exposure warnings",
        "Maturity, SIP-due and dividend-received alerts",
        "Tax-harvesting opportunity detection — never regulated advice",
      ]}
    />
  );
}
